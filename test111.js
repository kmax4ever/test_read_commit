// const formatDateToYyMmDd = (date) => {
//   const yy = String(date.getFullYear()); // Get last 2 digits of the year
//   const mm = String(date.getMonth() + 1).padStart(2, "0"); // Get month (1-12) and pad with zero
//   const dd = String(date.getDate()).padStart(2, "0"); // Get day and pad with zero
//   return `${yy}-${mm}-${dd}`; // Return the formatted string
// };
